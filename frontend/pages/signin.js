import React from 'react';
import Layout from '../components/Layout';

const SignIn = () => {
    return (
        <Layout>
            <h1>Sign in</h1>
        </Layout>
    );
};

export default SignIn;